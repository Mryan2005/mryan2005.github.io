language: node_js
node_js:
- '10'
before_deploy:
- hexo clean
- hexo g
deploy:
  provider: pages
  skip_cleanup: true
  github_token: ${GITHUB_TOKEN}  # Set in the settings page of your repository, as a secure variable
  keep_history: true
  on:
    branch: master